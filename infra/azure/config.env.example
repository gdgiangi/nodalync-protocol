# Azure Bootstrap Node Configuration
# Copy to config.env and fill in your values
# DO NOT commit config.env to version control

# Azure settings
AZURE_SUBSCRIPTION=""          # Optional, uses default if not set
RESOURCE_GROUP="nodalync-bootstrap"
LOCATION="eastus"              # eastus, westeurope, southeastasia, etc.

# Resource names (must be globally unique, lowercase alphanumeric)
ACR_NAME="nodalyncacr"         # Azure Container Registry (3-50 chars)
STORAGE_ACCOUNT="nodalyncdata" # Storage account (3-24 chars)

# Container settings
CONTAINER_NAME="nodalync-bootstrap-1"
DNS_LABEL="nodalync-bootstrap" # => <label>.<location>.azurecontainer.io

# Node password (generate with: openssl rand -base64 32)
NODALYNC_PASSWORD=""

# Hedera Testnet Settlement (optional)
# When both ACCOUNT_ID and PRIVATE_KEY are set, the node will use real
# Hedera testnet settlement instead of mock settlement.
#
# IMPORTANT: Private key format depends on the account's key type.
# Check your account's key type on HashScan: https://hashscan.io/testnet/account/<account_id>
#
# For ECDSA_SECP256K1 accounts:
#   Use DER-encoded private key (starts with "3030020100300706052b8104000a...")
#   The raw hex key will be interpreted as Ed25519 and cause InvalidSignature errors.
#   Example: HEDERA_PRIVATE_KEY="3030020100300706052b8104000a04220420<32-byte-key-hex>"
#
# For ED25519 accounts:
#   Use raw hex private key (64 hex characters)
#   Example: HEDERA_PRIVATE_KEY="<64-hex-chars>"
#
# HEDERA_ACCOUNT_ID="0.0.7703962"
# HEDERA_PRIVATE_KEY="<your-private-key>"
# HEDERA_CONTRACT_ID="0.0.7729011"  # Default settlement contract
