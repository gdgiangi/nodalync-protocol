# Nodalync Environment Variables
# Copy this file to .env and fill in your values.
# DO NOT commit .env to version control -- it may contain secrets.

# =============================================================================
# Node Identity
# =============================================================================

# Password used to encrypt/decrypt the node's Ed25519 keypair on disk.
# Required for: init, start, publish, query, whoami
# Generate a strong one: openssl rand -base64 32
NODALYNC_PASSWORD=

# =============================================================================
# Logging
# =============================================================================

# Rust log level filter. Examples:
#   nodalync=info          (recommended for normal use)
#   nodalync=debug         (verbose, useful for troubleshooting)
#   nodalync=trace         (very verbose)
#   nodalync_net=debug     (debug only networking)
RUST_LOG=nodalync=info

# =============================================================================
# Hedera Settlement — CLI (optional)
# =============================================================================
# These HEDERA_* variables are read by the CLI settlement path (context.rs).
# When all three values are set the node uses real Hedera testnet settlement.
# If omitted, settlement features (paid queries) are unavailable.
#
# Note: The MCP server subcommand uses NODALYNC_HEDERA_* prefixed variants
# (e.g., NODALYNC_HEDERA_ACCOUNT_ID). See `nodalync mcp-server --help`.

# Hedera testnet account ID (e.g., 0.0.12345)
HEDERA_ACCOUNT_ID=

# Hedera private key (hex-encoded inline value, not a file path)
HEDERA_PRIVATE_KEY=

# Settlement contract ID deployed on Hedera testnet
HEDERA_CONTRACT_ID=0.0.7729011

# Override settlement network (testnet | mainnet | previewnet)
# HEDERA_NETWORK=testnet

# =============================================================================
# Docker Compose (local 3-node cluster)
# =============================================================================
# These are used by infra/local/docker-compose.yml and the root docker-compose.yml.

# Docker image to use for the local cluster
# NODALYNC_IMAGE=gabrielgiangi/nodalync:latest

# Settlement network for the local cluster (mock = no real blockchain)
# SETTLEMENT_NETWORK=mock

# =============================================================================
# Alerting (optional, for bootstrap/production nodes)
# =============================================================================

# Discord webhook URL for node alerts
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
